#include "main.h"

/**
 * mul - check if character is uppercase
 * @a: a is paramemter
 * @b: b is paramemter
 * Return: return 1 if uppcase and 0 otherwise
 */

int mul(int a, int b)
{

	return (a * b);

}
